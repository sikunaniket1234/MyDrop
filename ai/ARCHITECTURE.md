MyDrop is a distributed local-first synchronization platform.

Goals:

1. Data ownership
2. Offline first
3. No silent data loss
4. Device equality

Never introduce central server dependencies.

All synchronization logic belongs in mydrop-core.